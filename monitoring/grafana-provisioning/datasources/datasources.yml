apiVersion: 1

datasources:
  - name: 'Prometheus (MEV Bot)'
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  - name: 'Loki (MEV Logs)'
    type: loki
    access: proxy
    url: http://loki:3100